@page
@model Projet_AdoNet.Pages.Operations.FiltreProjetParDelaisModel
@using Microsoft.AspNetCore.Http
@{
    ViewData["Title"] = "Les projets par délais";
}


@*Test de redirection*@
@if (HttpContext.Session.GetString("login") == null)
{
    <script src="~/js/Redirect.js"></script>
}


<br />

<div class="text-center">
    <h1 class="display-4">@ViewData["Title"]</h1>
</div>

<br />


@*<br />

    <table class="table table-striped table caption-top">
        <thead>
            <tr>
                <th scope="col">Nombre de projet</th>
                <th scope="col">Ville</th>
                <th scope="col">Actions</th>

            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.ProjetPC)
            {
                <tr>
                    <th scope="row">@Html.DisplayFor(Model => item.NombreProjet)</th>
                    <td>@Html.DisplayFor(Model => item.Ville)</td>
                    <td><a asp-page="./DetailsProjetParVille" asp-route-ville="@item.Ville">Tous les projets de la villa</a></td>
                </tr>
            }
        </tbody>
    </table>*@